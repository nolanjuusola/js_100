/*
6.
Will this program produce an error when run? Why or why not?

const FOO = 'bar';
{
  const FOO = 'qux';
}

console.log(FOO);


It will not produce an error, because the attempted re-asignment of the
const varaible is happening inside the blocks.
*/

/*
6.
What does this code output to the console?

function isArrayEmpty(arr) {
  if (arr) {
    console.log('Not Empty');
  } else {
    console.log('Empty');
  }
}

isArrayEmpty([]);

The code will log 'Not Empty' since, even 
though the array is in fact empty, an empty array
 is a truty value in JavaScript, so the first
 branch of the if statement will be the one to 
 execute.

 */

